# ARGUS - Unified Dependencies
# ========================

# Web Frameworks
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Computer Vision
opencv-python>=4.9.0
# numpy 2.x: the Pi's apt picamera2 is built against 2.x, so pinning <2
# breaks the camera. Inside a --system-site-packages venv pip will think
# the system copy satisfies this -- use --ignore-installed to override.
numpy>=2.2,<3
# ncnn imports portalocker at export time and does not declare it.
portalocker>=2.8.0
Pillow>=10.2.0
scipy>=1.11.0

# Machine Learning & AI
ultralytics>=8.4.0
torch>=2.0.0
torchvision>=0.15.0

# Face recognition is DEPRECATED (2026-09-14) and no longer installed by
# default. insightface compiles from source on a Pi and the stage is
# off. To re-enable: pip install -r requirements-faces.txt and set
# FACE_RECOGNITION_ENABLED=true.


# Federated Learning
flwr>=1.13.0

# Monitoring & Metrics
prometheus-client>=0.19.0
psutil>=5.9.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=42.0.0

# Database
aiosqlite>=0.19.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.26.0
streamlit>=1.31.0
PyYAML>=6.0.0
requests>=2.31.0

# Development
# markdown backs scripts/build_docs_pdf.py, which renders docs/*.md to the
# handout PDFs. Undeclared until it failed a rebuild.
markdown>=3.5
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.1.0
ruff>=0.1.0
